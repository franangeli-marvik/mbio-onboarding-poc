[project]
name = "mbio-backend"
version = "0.1.0"
description = "MBIO Profile Creation Backend with LangGraph Interview Prep Pipeline"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
